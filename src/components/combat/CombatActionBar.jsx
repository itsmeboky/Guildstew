/**
 * SHIM — file relocated to src/game-packs/dnd5e/ui/CombatActionBar.jsx as
 * part of Combat Engine v2 Phase 1 rebuild. First UI component relocation;
 * same shim pattern as Phase 1.2 but for a default-export React component.
 * Re-exports preserve all imports working unchanged. Will be removed in
 * Phase 8 cleanup.
 *
 * See: combat_redo_plan_v3.md (Part 8) and Phase 1.2 commit for the pattern.
 */
export { default } from '@/game-packs/dnd5e/ui/CombatActionBar.jsx';
